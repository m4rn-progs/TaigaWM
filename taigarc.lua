Keybinds = {
    "alt+shift space spawn kitty",
    "alt q killactive",
    "alt escape exit",
} -- valid config

Autostart = {
    "kitty",
} -- valid config

Libinput = {
    tap_to_click = false,
    accel_profile = "flat",
    -- etc
} -- invalid config

Xkb = {
    layout = "us",
    variant = "dvorak",
    -- etc
} -- invalid config

Misc = {
    tearing = true,
    -- etc
} -- valid config
